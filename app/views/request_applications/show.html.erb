<p id="notice"><%= notice %></p>

<p>
  <strong>Management no:</strong>
  <%= @request_application.management_no %>
</p>

<p>
  <strong>Emargency:</strong>
  <%= @request_application.emargency %>
</p>

<p>
  <strong>Filename:</strong>
  <%= @request_application.filename %>
</p>

<p>
  <strong>Request date:</strong>
  <%= @request_application.request_date %>
</p>

<p>
  <strong>Preferred date:</strong>
  <%= @request_application.preferred_date %>
</p>

<p>
  <strong>Close:</strong>
  <%= @request_application.close %>
</p>

<p>
  <strong>Memo:</strong>
  <%= simple_format(@request_application.memo) %>
</p>

<hr>

<h3>履歴</h3>

<table class="table">
  <thead>
    <tr>
      <th>History_no</th>
      <th>Order</th>
      <th>Dept</th>
      <th>Start</th>
      <th>End</th>
    </tr>
  </thead>

  <tbody>
    <% @request_application.flows.each do |flow| %>
      <tr>
        <td><%= flow.history_no %></td>
        <td><%= flow.order %></td>
        <td><%= flow.dept.try(:name) %></td>
         <td><%= flow.progress.try(:in_date) %></td>
         <td><%= flow.progress.try(:out_date) %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'Edit', edit_request_application_path(@request_application) %> |
<%= link_to 'Back', request_applications_path %>
